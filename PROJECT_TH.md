# HydroMonitor — เอกสารภาพรวมโครงการ (ฉบับภาษาไทย)

> **การพัฒนาดัชนีอัจฉริยะด้วยปัญญาประดิษฐ์ เพื่อประเมินความมั่นคงทางน้ำของโรงเรียนสาธิตมหาวิทยาลัยเชียงใหม่**
>
> *Development of an AI-Powered Smart Index for School Water Security Assessment: A Case Study of Chiang Mai University Demonstration School*

เอกสารฉบับนี้เรียบเรียงบริบทของโครงการทั้งหมด โดยยึดโครงเรื่องตามงานนำเสนอ (Problem & Pain → Solution → Prototype → Dashboards → Results → Impact) และเติมรายละเอียดเชิงเทคนิคจากโค้ดจริงในรีโพนี้

---

## สารบัญ

1. [ปัญหาและความเจ็บปวด (Problem & Pain)](#1-ปัญหาและความเจ็บปวด-problem--pain)
2. [แนวทางแก้ปัญหา (Solution)](#2-แนวทางแก้ปัญหา-solution)
3. [ภาพรวมระบบ (System Overview)](#3-ภาพรวมระบบ-system-overview)
4. [ฮาร์ดแวร์และเฟิร์มแวร์ (ESP32)](#4-ฮาร์ดแวร์และเฟิร์มแวร์-esp32)
5. [แบ็กเอนด์ (main.py / FastAPI)](#5-แบ็กเอนด์-mainpy--fastapi)
6. [การปรับเทียบเซนเซอร์ (Calibration)](#6-การปรับเทียบเซนเซอร์-calibration)
7. [เซนเซอร์วัดการไหลและการใช้น้ำ (Flow Sensor)](#7-เซนเซอร์วัดการไหลและการใช้น้ำ-flow-sensor)
8. [การเก็บประวัติข้อมูล (History)](#8-การเก็บประวัติข้อมูล-history)
9. [การแจ้งเตือนแบบ Push Notification](#9-การแจ้งเตือนแบบ-push-notification)
10. [การตั้งค่า WiFi ผ่านสาย USB](#10-การตั้งค่า-wifi-ผ่านสาย-usb)
11. [หน้าจอแดชบอร์ด (Frontend)](#11-หน้าจอแดชบอร์ด-frontend)
12. [วิธีติดตั้งและใช้งาน (Prototype Setup)](#12-วิธีติดตั้งและใช้งาน-prototype-setup)
13. [ผลลัพธ์และผลกระทบ (Results & Impact)](#13-ผลลัพธ์และผลกระทบ-results--impact)
14. [ข้อจำกัดและแนวทางพัฒนาต่อ](#14-ข้อจำกัดและแนวทางพัฒนาต่อ)

---

## 1. ปัญหาและความเจ็บปวด (Problem & Pain)

งานนำเสนอใช้ภาพเปรียบเทียบ **ภูเขาน้ำแข็ง** เพื่ออธิบายว่าปัญหาน้ำในสถานศึกษามีทั้งส่วนที่เห็นและส่วนที่ซ่อนอยู่

### ปัญหาที่มองเห็นบนยอดภูเขาน้ำแข็ง

- บิลค่าน้ำพุ่งสูงขึ้นโดยไม่ทราบสาเหตุ
- ท่อแตกหรือน้ำรั่วซึม ที่มักถูกพบ **โดยบังเอิญ** ไม่ใช่จากการตรวจสอบอย่างเป็นระบบ
- วัฒนธรรมการทำงานแบบ "รอให้เกิดปัญหาก่อน แล้วจึงค่อยแก้"

### ปัญหาที่ซ่อนอยู่ใต้ยอดภูเขาน้ำแข็ง (รากของปัญหาจริง)

- **ไม่สามารถประเมินความเสี่ยงและรับมือล่วงหน้าได้** — ไม่มีข้อมูลเชิงปริมาณที่ต่อเนื่องพอจะใช้พยากรณ์
- **ขาดระบบตรวจจับความผิดปกติแบบอัตโนมัติ** — ต้องอาศัยคนเดินตรวจ ซึ่งทำได้ไม่ต่อเนื่องและไม่ทั่วถึง
- **ขาดเครื่องมือประเมินเชิงรุก** — ไม่มีดัชนีกลางที่บอกได้ว่า "ตอนนี้สถานะความมั่นคงทางน้ำของโรงเรียนอยู่ระดับใด"

ผลลัพธ์คือ น้ำสะอาดสูญเสียไปกับการรั่วซึมใต้ดินที่มองไม่เห็น งบประมาณค่าน้ำรั่วไหลไปเรื่อย ๆ และคุณภาพน้ำที่นักเรียนใช้ก็ไม่มีหลักฐานยืนยัน

---

## 2. แนวทางแก้ปัญหา (Solution)

แนวคิดหลักของโครงการมี 3 ข้อ ตามที่ระบุในสไลด์

| เปลี่ยนจาก | ไปเป็น |
|---|---|
| รอให้เกิดปัญหาแล้วค่อยแก้ | **ตรวจจับและรับมือล่วงหน้า** (proactive) |
| ใช้แรงงานคนเดินตรวจ | **ใช้ระบบอัตโนมัติ / ปัญญาประดิษฐ์** ประมวลผลข้อมูลแทน |
| สถานศึกษาแบบเดิม | **Smart Campus ที่มีมาตรฐาน** วัดผลได้ |

การนำไปใช้จริงคือการวางระบบตรวจวัดคุณภาพน้ำและปริมาณการใช้น้ำแบบเรียลไทม์ แล้วสรุปออกมาเป็น **ดัชนีคุณภาพน้ำ (WQI — Water Quality Index)** พร้อมกราฟย้อนหลังและการแจ้งเตือนอัตโนมัติ เพื่อให้ผู้ดูแลอาคารเห็นความผิดปกติ *ก่อน* ที่มันจะกลายเป็นค่าใช้จ่าย

---

## 3. ภาพรวมระบบ (System Overview)

ระบบประกอบด้วย 3 ส่วนหลักที่ทำงานต่อกันเป็นสายโซ่

```
[ ESP32 + เซนเซอร์ 4 ตัว ]
        │  POST /update  (JSON ค่าดิบทุก ๆ 2 วินาที)
        ▼
[ แบ็กเอนด์ main.py (FastAPI) ]
        │  ├── แปลงค่าดิบ → ค่าจริง ด้วยค่าคาลิเบรต
        │  ├── บันทึกลง SQLite (history.db)
        │  ├── ส่งต่อไป Google Sheets (สำรอง)
        │  ├── ตรวจเกณฑ์ → ยิง Web Push แจ้งเตือน
        │  └── กระจายผ่าน WebSocket /ws/app
        ▼
[ แดชบอร์ดบนเบราว์เซอร์ (React + Vite) ]
```

**หลักการออกแบบสำคัญ:** เฟิร์มแวร์บน ESP32 ส่งเฉพาะ **ค่าดิบ (raw)** เท่านั้น สูตรแปลงค่าทั้งหมดอยู่ฝั่งแบ็กเอนด์ ทำให้ปรับเทียบเซนเซอร์ใหม่ได้ทันทีโดย **ไม่ต้องแฟลชบอร์ดใหม่**

ไฟล์สำคัญ

| ไฟล์ | หน้าที่ |
|---|---|
| `firmware/esp32/esp32.ino` | เฟิร์มแวร์บนบอร์ด ESP32 |
| `main.py` | แบ็กเอนด์เดียวของระบบ (FastAPI) |
| `storage.py` | จัดการฐานข้อมูล SQLite |
| `thresholds.py` | เกณฑ์ดี/เฝ้าระวัง/อันตราย ของแต่ละพารามิเตอร์ |
| `webconfig.json` | ไฟล์ตั้งค่าทั้งระบบ |
| `calibration.json` | ค่าสัมประสิทธิ์การปรับเทียบ (สร้างเมื่อบันทึกครั้งแรก) |
| `frontend/` | ซอร์สโค้ดแดชบอร์ด (Vite + React 19 + TypeScript) |
| `web/calibrate.html` | หน้าปรับเทียบแบบสแตนด์อโลน |
| `google_apps_script.gs` | สคริปต์ฝั่ง Google Sheets (ใช้อ้างอิง ไม่ได้รันจากรีโพ) |

---

## 4. ฮาร์ดแวร์และเฟิร์มแวร์ (ESP32)

ใช้บอร์ด **ESP32 ตัวเดียว** (ไม่ใช่ ESP32-CAM — โครงการนี้ไม่มีการประมวลผลภาพ) อ่านเซนเซอร์ 4 ตัวทุก ๆ 2 วินาที แล้ว POST เป็น JSON

```json
{ "temperature": 27.5, "turbidity": 2100, "tdsVoltage": 0.83, "flowPulses": 145 }
```

### เซนเซอร์และการต่อสาย

| เซนเซอร์ | ขา | ต่อกับ ESP32 | หมายเหตุ |
|---|---|---|---|
| DS18B20 (อุณหภูมิ) | VCC | 3.3V | ให้สัญญาณ OneWire อยู่ที่ระดับ 3.3V ที่ปลอดภัย |
| | DATA | GPIO13 | ต้องมีตัวต้านทาน pull-up 4.7kΩ ไป 3.3V — **ห้ามใช้ GPIO12** เพราะเป็นขา boot-strapping อาจทำให้บอร์ดบูตไม่ขึ้น |
| เซนเซอร์ความขุ่น | VCC | 5V / VIN | ต้องการไฟ 5V จริงจึงจะได้เส้นโค้งตามสเปก |
| | OUT | → วงจรแบ่งแรงดัน → GPIO34 | GPIO34 เป็น ADC1 (อินพุตอย่างเดียว) เลี่ยงขา ADC2 เพราะ WiFi จะปิดการทำงานของ ADC2 |
| TDS Meter V1.0 | VCC | 3.3V | รับได้ 3.3–5.5V แรงดันขาออกไม่ขึ้นกับไฟเลี้ยง |
| | A (สัญญาณ) | GPIO35 (ต่อตรง) | ขาออกสูงสุด ~2.3V ต่ำกว่าลิมิต 3.3V |
| YF-S201 (วัดการไหล) | VCC | 5V / VIN | เซนเซอร์ฮอลล์เอฟเฟกต์ต้องการ 5V |
| | Signal | GPIO27 (`INPUT_PULLUP`) | เป็น **สัญญาณดิจิทัล** ไม่ใช่แอนะล็อก จึงใช้ขา ADC2 ได้ |

**วงจรแบ่งแรงดัน (เฉพาะความขุ่น):** R1 = 10kΩ (OUT → จุดกลาง), R2 = 20kΩ (จุดกลาง → GND), จุดกลาง → GPIO34 ลดช่วง 0–4.5V ลงเหลือ 0–3.0V แล้วเฟิร์มแวร์คูณกลับ 1.5 เท่า (`dividerRecoveryFactor`)

### กลไกพิเศษในเฟิร์มแวร์

- **ค้นหาแบ็กเอนด์อัตโนมัติ (`discoverBackend`)** — บรอดแคสต์ `HYDRO_DISCOVER` ผ่าน UDP พอร์ต 8888 แล้วรับ IP ของเครื่องที่ตอบกลับ ทำให้ไม่ต้องฝัง IP ไว้ในโค้ด และรอด DHCP เปลี่ยน IP ได้ ถ้า POST ล้มเหลว 3 ครั้งติดจะค้นหาใหม่อัตโนมัติ (จำกัดเวลารอที่ 20 วินาที ไม่ค้างตลอดไป)
- **สำรองส่งเข้า Google Sheets โดยตรง** — ถ้าติดต่อแบ็กเอนด์ไม่ได้แต่อินเทอร์เน็ตยังใช้ได้ ข้อมูลจะถูกเก็บในบัฟเฟอร์วนรอบ 30 รายการ แล้วส่งตรงเข้า Apps Script ทุก 60 วินาที ทำให้ข้อมูลไม่หายแม้แบ็กเอนด์ล่ม (ส่งเฉพาะอุณหภูมิ/ความขุ่น/TDS — ไม่รวมค่าการไหล)
- **นับพัลส์ด้วยอินเทอร์รัปต์** — `attachInterrupt(..., RISING)` พร้อม `portMUX_TYPE` ป้องกันการชนกันระหว่างสองคอร์ของ ESP32 ตัวนับจะถูกอ่านและรีเซ็ตทุกรอบ ทำให้ `flowPulses` หมายถึง "จำนวนพัลส์นับตั้งแต่ POST ครั้งก่อน" เสมอ

---

## 5. แบ็กเอนด์ (main.py / FastAPI)

`main.py` เป็นแบ็กเอนด์ **เพียงตัวเดียว** ของระบบ ทำหน้าที่

- **รับข้อมูลเซนเซอร์** — `POST /update` รับได้ทั้ง JSON และ form-urlencoded รองรับเฟิร์มแวร์รุ่นเก่าที่ยังไม่มี `flowPulses` ระบบจะเติม `source`/`timestamp` คำนวณค่า **EC (ค่าการนำไฟฟ้า, µS/cm)** จาก TDS แล้วกระจายผ่าน WebSocket
- **กระจายข้อมูลสู่แดชบอร์ด** — `WS /ws/app` ทุกเบราว์เซอร์ที่เปิดหน้าแดชบอร์ดจะเชื่อมต่อที่นี่และรับข้อความ `sensor_update`
- **บริการค้นหาแบ็กเอนด์ผ่าน UDP** — ฟังพอร์ต 8888 ตอบกลับ `HYDRO_HERE` (ต้องเปิด Windows Firewall ให้ UDP ขาเข้าพอร์ต 8888 มิฉะนั้นการค้นหาจะเงียบไม่มีผล)
- **บันทึกลง SQLite และส่งต่อ Google Sheets** — ทั้งคู่ทำแบบ fire-and-forget ผ่าน `asyncio.to_thread` ดังนั้นดิสก์ช้าหรือ Google ล่มก็ไม่ทำให้ ESP32 ต้องรอ

### พอร์ตและการตั้งค่า

- HTTP `0.0.0.0:8080` — เปิดตลอด (ESP32 POST มาที่นี่)
- HTTPS `0.0.0.0:8443` — เปิดเพิ่มเมื่อกำหนด `httpsCertFile`/`httpsKeyFile` ใน `webconfig.json` (จำเป็นสำหรับ Web Push บนอุปกรณ์อื่นนอกจาก localhost)
- **Autoreload ปิดโดยค่าเริ่มต้น** เปิดได้ด้วย `HYDRO_DEV=1` เท่านั้น — เพราะตัว reloader เฝ้าดูโฟลเดอร์ทำงาน ทุกครั้งที่บันทึกค่าคาลิเบรตหรือเขียนประวัติจะทำให้เซิร์ฟเวอร์รีสตาร์ตและ WebSocket หลุดหมด
- **ความปลอดภัยของ `/update`** — ถ้าตั้ง `updateApiKey` ใน `webconfig.json` ระบบจะบังคับให้ส่ง header `X-API-Key` ที่ตรงกัน (เทียบแบบ constant-time) เรื่องนี้สำคัญมากเมื่อเปิดให้บอร์ดเข้าถึงแบ็กเอนด์จากภายนอกวง LAN มิฉะนั้นใครก็ตามที่เจอ endpoint นี้จะส่งค่าปลอมเข้ามาได้

### การติดตั้งเป็นเซอร์วิส

`scripts/install-service.ps1` ติดตั้ง `main.py` เป็น Windows Service ผ่าน NSSM (รีสตาร์ตอัตโนมัติเมื่อล่ม, เก็บ log หมุนเวียนใน `logs/`) — ถ้าไม่ทำ ระบบจะเป็นแค่หน้าต่างเทอร์มินัลที่ปิดไปเงียบ ๆ เมื่อรีบูตเครื่อง

---

## 6. การปรับเทียบเซนเซอร์ (Calibration)

ค่าคาลิเบรตทั้งหมดอยู่ที่ **แบ็กเอนด์** ไม่ใช่เฟิร์มแวร์ จึงปรับได้สดโดยไม่ต้องแฟลชบอร์ด บันทึกลง `calibration.json`

### รูปแบบการปรับเทียบแต่ละตัว

| เซนเซอร์ | โมเดล | ต้องการ |
|---|---|---|
| ความขุ่น (Turbidity) | เส้นตรง 2 จุด `ntu = slope·adc + intercept` (ค่า slope ติดลบ เพราะ ADC สูง = น้ำใสกว่า) | อย่างน้อย 2 จุด ถ้าไม่ครบจะไม่แสดงค่า NTU |
| TDS | สูตรพหุนามชดเชยอุณหภูมิของ DFRobot × **ค่า k เดียว** | น้ำอ้างอิงที่ทราบค่า ppm 1 จุด |
| การไหล (Flow) | ค่า k เดียว = จำนวนพัลส์ต่อลิตร (YF-S201 ค่ามาตรฐาน ≈ 450) | การเทน้ำปริมาตรที่ทราบค่า 1 ครั้ง |

### สวิตช์ Calibration Mode

ค่าที่บันทึกไว้จะ **มีผลก็ต่อเมื่อเปิด Calibration Mode** เท่านั้น (ปุ่มเปิด/ปิดในหน้า `/calibrate`) ถ้าปิดอยู่ `/update` จะส่งค่า ADC ดิบ (`turbidityUnit: "ADC"`) และค่า ppm แบบ k=1.0 เสมอ — ออกแบบมาเพื่อไม่ให้ค่าคาลิเบรตที่ยังไม่ได้ตรวจสอบเข้าไปกระทบแดชบอร์ดจริงโดยไม่ตั้งใจ

### หน้าและ API

- `GET /calibrate` — หน้าปรับเทียบสแตนด์อโลน โพลค่าดิบทุก ~1 วินาที ระบบจะเฉลี่ยค่าดิบ 5 ค่าล่าสุดตอนบันทึกจุด เพื่อลด noise มีปุ่ม "use min"/"use max" จากช่วงที่สังเกตได้ และปุ่ม "Reset range" ที่ล้างเฉพาะการติดตามฝั่งไคลเอนต์ ไม่แตะค่าที่บันทึกไว้
- REST: `GET /calibration`, `POST /calibration/mode`, `POST /calibration/capture`, `DELETE /calibration/point`, `POST /calibration/save`, `POST /calibration/reset` (ค่า `sensor` = `"turbidity"` / `"tds"` / `"flow"`) — ทุกการแก้ไขอยู่ในหน่วยความจำจนกว่าจะกด save

---

## 7. เซนเซอร์วัดการไหลและการใช้น้ำ (Flow Sensor)

นี่คือส่วนที่ตอบโจทย์ "ท่อรั่วซึมใต้ดินที่มองไม่เห็น" โดยตรง เป็นเซนเซอร์หลักตัวที่ 5 แต่ **ไม่ใช่พารามิเตอร์คุณภาพน้ำ** จึงไม่มีเกณฑ์ดี/เฝ้าระวัง/อันตราย และไม่มีการแจ้งเตือน push

แบ็กเอนด์แปลงจำนวนพัลส์เป็น 2 ค่า

1. **อัตราการไหลปัจจุบัน** (`flowRate`, ลิตร/นาที) — ค่าชั่วขณะ แสดงสด เก็บลงคอลัมน์ `readings.flow_rate`
2. **ปริมาณการใช้น้ำวันนี้** (`waterUsageToday`, ลิตร) — ตัวนับสะสมที่ **รีเซ็ตทุกวัน** เก็บในตาราง `daily_usage(date, total_liters)` แยกต่างหาก การขึ้นวันใหม่จะสร้างแถวใหม่เองโดยไม่ต้องมี scheduler และมี `POST /flow/reset-today` สำหรับรีเซ็ตด้วยตนเอง

`GET /flow/usage?days=N` คืนยอดวันนี้พร้อมยอดรวมย้อนหลัง N วัน สำหรับกราฟแท่ง "Water Usage" บนแดชบอร์ด

> **หมายเหตุ:** **อัตราการไหล (`flowRate`) ถูกบันทึกลง Google Sheets แล้ว** ในคอลัมน์ `Flow Rate (L/min)` แต่ **ปริมาณการใช้น้ำรายวันไม่ถูกบันทึก** เพราะเป็นข้อมูลคนละรูปทรง (หนึ่งแถวต่อวัน) ที่ไม่เข้ากับ log แบบรายค่าอ่าน — ดูได้จาก `GET /flow/usage` เท่านั้น
>
> แถวที่บันทึกไว้ก่อนเพิ่มคอลัมน์นี้ (หรือมาจากแหล่งที่ไม่มีค่าการไหล) จะได้ `flowRate: null` และเส้นทางสำรองที่ ESP32 ส่งตรงเข้า Sheets เองยัง **ไม่ส่ง** ค่าการไหล — บันทึกแค่อุณหภูมิ/ความขุ่น/TDS

---

## 8. การเก็บประวัติข้อมูล (History)

`GET /history?window=` รองรับช่วงเวลา `5m` / `15m` / `1h` / `3h` / `12h` / `24h` (ค่าเริ่มต้น 15m) ดึงข้อมูลจาก **3 ชั้น เรียงจากถูกที่สุดก่อน** แล้วรวมเป็นลำดับเวลาเดียว (ฟิลด์ `source` บอกว่าชั้นไหนมีส่วนร่วม เช่น `db`, `db+sheet`, `live`)

1. **SQLite ในเครื่อง** (`storage.py`, โหมด WAL) — ตอบได้ทุกช่วงเวลาและรอดการรีสตาร์ต จึงเป็นตัวเลือกแรกเสมอ
2. **บัฟเฟอร์ในหน่วยความจำ** — ใช้เฉพาะแถวที่ใหม่กว่าใน DB (กรณีเขียน DB ล้มเหลว) หรือใช้ทั้งหมดเมื่อปิด DB
3. **Google Sheets** — เรียกใช้เฉพาะช่วงที่สองชั้นแรกไปไม่ถึง เช่น DB เพิ่งสร้างใหม่ หรือข้อมูลเก่ากว่าที่ DB มี

การตรวจ "ช่องว่างข้อมูล" ยอมให้คลาดเคลื่อนได้ 15 วินาที (`HISTORY_GAP_TOLERANCE_MS`) เพราะข้อมูลเข้ามาทุก 2 วินาที ถ้าเทียบแบบตรงเป๊ะ ทุกคำขอในสภาวะปกติจะดูเหมือนมีช่องว่างและไปเรียก Sheets โดยไม่จำเป็น

ทุกแถวจากทุกแหล่งจะถูกเติม `turbidityNtu` (คำนวณจาก ADC ดิบ) และ `ec` ให้เหมือนกันหมด — หมายความว่า **ข้อมูลเก่าจะถูกคำนวณค่า NTU ใหม่ตามค่าคาลิเบรตล่าสุดเสมอตอนอ่าน** ไม่ต้องแก้ข้อมูลย้อนหลัง

### Google Apps Script

`google_apps_script.gs` เป็นไฟล์อ้างอิง ต้องนำไปวางใน **Extensions > Apps Script** ของสเปรดชีตเป้าหมาย แล้ว deploy เป็น Web App (Execute as: Me, Who has access: Anyone) คอลัมน์คือ `Timestamp, Temperature (C), Turbidity (raw ADC), TDS (ppm), Flow Rate (L/min)` และแทรกข้อมูลใหม่ที่ **แถวที่ 2** เสมอ เพื่อให้ค่าล่าสุดอยู่บนสุด (แถวเก่าที่เขียนก่อนมีคอลัมน์ Flow Rate จะมีแค่ 4 ช่อง `doGet` จะอ่านเป็น `null` ให้เอง)

> **ข้อควรระวัง:** หลังแก้ `doGet`/`doPost` ต้อง deploy เป็น **เวอร์ชันใหม่** มิฉะนั้น `/exec` จะยังเสิร์ฟโค้ดเดิม

---

## 9. การแจ้งเตือนแบบ Push Notification

ระบบแจ้งเตือนระดับ OS เมื่อค่าหลุดเกณฑ์ ทำให้ได้รับการเตือนแม้ไม่ได้เปิดหน้าแดชบอร์ดไว้

- **VAPID keys** — ตั้งค่าใน `webconfig.json` (`vapidPrivateKeyFile`, `vapidPublicKey`, `vapidSubject`) สร้างคีย์ด้วย `vapid --gen` ถ้ายังไม่ได้ตั้งค่า ทุก endpoint `/push/*` จะตอบ 503 แทนที่จะทำให้ระบบพัง
- **ต้องใช้ HTTPS** — Web Push ต้องการ secure context และ `http://localhost` ยกเว้นให้เฉพาะเครื่องเดียวกันเท่านั้น มือถือบนวง LAN จึงต้องใช้ HTTPS จริง (self-signed ใช้ทดสอบได้แต่ต้อง trust CA รายเครื่อง — Android เข้มเป็นพิเศษ; ใช้ tunnel เช่น `cloudflared` หรือ Tailscale จะง่ายกว่า)
- **เงื่อนไขการยิง** — `_check_breaches_and_dispatch` ทำงานทุกครั้งที่มีข้อมูลเข้า แต่จะยิงเฉพาะตอน **เปลี่ยนสถานะจาก good → warn/danger** ไม่ใช่ยิงซ้ำทุกครั้งที่ยังผิดปกติ และจะพร้อมยิงใหม่เมื่อค่ากลับสู่ปกติ
- **เนื้อหาแจ้งเตือน** — เช่น `"🌡️ Temperature — Warn"` / `"33.1 °C is in the warn range"` พร้อมปุ่ม view / dismiss ถ้า push service ตอบ 404/410 ระบบจะลบ subscription นั้นทิ้งเอง
- **การตั้งค่า** — อยู่ในกล่อง Settings (ไอคอนเฟือง) ไม่ใช่ไอคอนกระดิ่งแยก มีปุ่ม "Send test notification" ให้ทดลองดูหน้าตาการแจ้งเตือนได้ทันที

---

## 10. การตั้งค่า WiFi ผ่านสาย USB

ปัญหาคือ: ตอนที่ต้องเปลี่ยนรหัส WiFi บอร์ดอาจไม่มีเน็ตเลย ช่องทางเดียวที่เหลือคือ **สาย USB**

### ช่องทางปัจจุบัน — เบราว์เซอร์คุยกับบอร์ดโดยตรง

`WifiPanel.tsx` ใช้ **Web Serial API** (`navigator.serial`) คุยกับ ESP32 โดยตรง ไม่ผ่านแบ็กเอนด์เลย กดปุ่ม "Connect to board" จะเปิดหน้าต่างเลือกอุปกรณ์ USB ของเบราว์เซอร์เอง หลังจากนั้นทุกคำสั่งวิ่งจากแท็บเบราว์เซอร์ไปที่บอร์ดตรง ๆ แปลว่าตั้งค่า WiFi ได้จากเครื่องใดก็ได้ที่เปิดหน้านี้ **โดยไม่ต้องมี `python main.py` รันอยู่ที่ไหนเลย**

> ใช้ได้เฉพาะเบราว์เซอร์ตระกูล Chromium (Chrome/Edge/Opera) และต้องอยู่บน `https://` หรือ `http://localhost` — เบราว์เซอร์อื่นจะเห็นข้อความอธิบายแทนหน้าจอที่พังเงียบ ๆ

### โปรโตคอลฝั่งเฟิร์มแวร์

เป็นโปรโตคอลแบบบรรทัด ทุกบรรทัดที่เครื่องอ่านขึ้นต้นด้วย `WIFI_` หรือ `BACKEND_` เพื่อไม่ให้ชนกับข้อความ debug ปกติ

| คำสั่ง | ผล |
|---|---|
| `WIFI_SCAN` | สแกนเครือข่าย ตอบ `WIFI_NET\|<ssid>\|<rssi>\|<encrypted>` ต่อรายการ แล้วปิดท้าย `WIFI_SCAN_DONE` |
| `WIFI_SET\|<ssid>\|<password>` | **ลองเชื่อมต่อก่อน** แล้วบันทึกลง NVS เฉพาะเมื่อเชื่อมต่อสำเร็จ — รหัสผิดจึงไม่ทับเครือข่ายเดิมที่ใช้งานได้ |
| `WIFI_STATUS` | รายงานสถานะการเชื่อมต่อ |
| `BACKEND_SET\|<host>\|<apiKey>\|<https>` | ตั้งแบ็กเอนด์แบบตายตัว ข้ามการค้นหาผ่าน UDP |
| `BACKEND_CLEAR` | ลบค่าที่ตั้งไว้ กลับไปค้นหาอัตโนมัติในวง LAN |
| `BACKEND_STATUS` | รายงานสถานะ (ไม่เคยส่งค่า API key กลับมา เช่นเดียวกับรหัส WiFi) |
| `BACKEND_TEST` | ให้ **บอร์ดเอง** ยิง `GET /update/health` ผ่านเครือข่าย/DNS/TLS ของตัวเอง — นี่คือสิ่งที่ทำให้การทดสอบมีความหมาย เพราะการทดสอบจากเครื่องแบ็กเอนด์เองบอกไม่ได้ว่าเส้นทางของบอร์ดใช้งานได้จริงหรือไม่ |

คำสั่งถูกอ่านแบบ non-blocking ทุกรอบ `loop()` และระหว่างรอเชื่อม WiFi ใน `setup()` ด้วย ดังนั้นบอร์ดตั้งค่าใหม่ได้ตลอดเวลา — รวมถึงตอนบูตขึ้นมาแล้วพบว่ารหัสที่เก็บไว้ผิด โดยไม่ต้องแฟลชใหม่

### กรณีบอร์ดอยู่คนละเครือข่ายกับแบ็กเอนด์

การค้นหาผ่าน UDP ทำงานได้เฉพาะใน subnet เดียวกัน ถ้าบอร์ดอยู่คนละที่ ต้องใช้ `BACKEND_SET` ระบุ host ตรง ๆ พร้อม API key และเปิด HTTPS (POST ไป `https://<host>:8443/update` ผ่าน `WiFiClientSecure` แบบ `setInsecure()` — เข้ารหัสข้อมูลระหว่างทาง แต่ไม่ตรวจใบรับรอง ซึ่งเป็นการแลกที่ยอมรับได้สำหรับงานระดับนี้)

> **สำคัญ:** ฟีเจอร์นี้แค่บอกที่อยู่ให้บอร์ด — เครื่องแบ็กเอนด์ยัง **ต้องเข้าถึงได้จริง** จากเครือข่ายของบอร์ด (port-forward + DDNS, VPN/Tailscale, หรือ tunnel) การตั้ง `apiKey` และเปิด HTTPS คือสิ่งที่กันไม่ให้คนอื่นส่งค่าปลอมเข้ามา

---

## 11. หน้าจอแดชบอร์ด (Frontend)

**Stack:** Vite + React 19 + TypeScript + Tailwind v4, shadcn/ui + Recharts + Motion + TanStack Query

มีเมนูด้านซ้าย 3 แท็บ

### แท็บ Dashboard
- กราฟประวัติ **WQI** พร้อมตัวเลือกช่วงเวลาและเส้นอ้างอิง
- **ตารางการ์ดพารามิเตอร์** — Temperature / Turbidity / TDS / EC / Flow พร้อมกราฟจิ๋ว (sparkline) 30 วินาที เลือกซ่อน/แสดงแต่ละตัวได้จาก Settings
- **คลิกการ์ดใด ๆ** เปิดหน้าต่างรายละเอียด: กราฟใหญ่, ค่า min/avg/max, และคำเตือนสองทิศทาง (สูงเกิน/ต่ำเกิน) — ยกเว้น Flow ที่ไม่มีเกณฑ์
- โหมด **Quick View** สรุปภาพรวม
- กราฟ **Water Flow** และ **Water Usage** ใต้ตารางการ์ด
- เกจวัดความปลอดภัยแบบเรเดียล 3 ตัว

### แท็บ Calibration
ปรับเทียบความขุ่นแบบ 2 จุด, ค่า k ของ TDS และค่า k ของ Flow ทั้งหมดต่อกับ API จริง มีการ apply แบบ optimistic พร้อม toast และแสดงช่วง min/max ที่สังเกตได้ — รวมถึงรายการ **WiFi** สำหรับตั้งค่าเครือข่ายผ่าน USB

### แท็บ History
ตารางข้อมูลย้อนหลังจาก `/history` พร้อม **ส่งออกเป็น CSV**

### ส่วนอื่น ๆ
- สลับธีม **สว่าง/มืด** (dark mode / light mode)
- สลับภาษา **ไทย / อังกฤษ** (ข้อความ UI ทั้งหมดรวมศูนย์ที่ `frontend/src/lib/strings.ts`)
- ทัวร์แนะนำการใช้งาน
- กล่อง **Settings** (ไอคอนเฟือง) รวมการตั้งค่าแจ้งเตือนและปุ่มซ่อน/แสดงการ์ดไว้ด้วยกัน

### หลักการเรื่องข้อมูลสด
`useSensorSocket.ts` ถือการเชื่อมต่อ `/ws/app` เพียงเส้นเดียวที่แชร์กันทั้งแอป และเก็บบัฟเฟอร์ตัวอย่างย้อนหลัง ~30 วินาทีสำหรับ sparkline โดยเติมข้อมูลเริ่มต้นจาก `GET /history?window=5m` ตอนโหลดหน้า เพื่อให้เห็นข้อมูลทันทีแทนกราฟว่าง

> **ไม่มีการสร้างข้อมูลปลอมเด็ดขาด** — เมื่อขาดการเชื่อมต่อหรือเงียบเกิน 5 วินาที ระบบจะขึ้นสถานะ "Offline" แล้วค้างค่าจริงล่าสุดไว้บนจอ ไม่สร้างตัวเลขที่ดูสมจริงขึ้นมาเอง เพราะสำหรับระบบเฝ้าระวัง ข้อมูลปลอมระหว่างเหตุขัดข้องเป็นอันตรายกว่าจอที่ค้าง

---

## 12. วิธีติดตั้งและใช้งาน (Prototype Setup)

ตามลำดับที่อธิบายในสไลด์ Prototype

### ขั้นที่ 1 — ติดตั้งและรันแบ็กเอนด์

```bash
pip install -r requirements.txt
python main.py
```

เสิร์ฟที่ `0.0.0.0:8080`

### ขั้นที่ 2 — build หน้าแดชบอร์ด

```bash
cd frontend
npm install
npm run build      # สร้าง frontend/dist/ ซึ่งถูก mount ที่ /
```

หน้า `/` จะขึ้น 404 จนกว่าจะ build อย่างน้อยหนึ่งครั้ง

### ขั้นที่ 3 — เชื่อมต่ออุปกรณ์วัดคุณภาพน้ำเข้ากับเครือข่าย

ต่อสาย USB จากบอร์ดเข้าเครื่อง เปิดแท็บ Calibration → WiFi → Connect to board → สแกน → เลือกเครือข่าย → ใส่รหัส → Connect

ต้องแน่ใจว่า Windows Firewall อนุญาต UDP ขาเข้าพอร์ต 8888:
```
netsh advfirewall firewall add rule name="HydroMonitor UDP Discovery" dir=in action=allow protocol=UDP localport=8888
```

### ขั้นที่ 4 — ปรับเทียบเซนเซอร์

เข้าหน้า `/calibrate` (หรือแท็บ Calibration) ปรับเทียบความขุ่นแบบ 2 จุดและค่า TDS แล้ว **เปิด Calibration Mode**

### ขั้นที่ 5 — ใช้งาน

เปิดหน้าแดชบอร์ดหลักเพื่อดูค่า WQI, กราฟเรียลไทม์, ตรวจสอบประวัติย้อนหลัง และส่งออก CSV ได้ทันที

### คำสั่งสำหรับการพัฒนา

```bash
HYDRO_DEV=1 python main.py    # เปิด autoreload (เฉพาะตอนพัฒนา)
cd frontend && npm run dev    # Vite dev server พร้อม proxy ไป :8080
cd frontend && npm run lint   # oxlint
```

> โครงการนี้ยังไม่มีชุดทดสอบอัตโนมัติหรือ linter ฝั่งแบ็กเอนด์

---

## 13. ผลลัพธ์และผลกระทบ (Results & Impact)

### ต่อตัวโรงเรียน

- **ประหยัดงบประมาณค่าน้ำประปา** ที่สูญเสียไปกับท่อรั่วซึมใต้ดินที่มองไม่เห็น
- เปลี่ยนแนวคิด **Micro-Model for CMU Smart Campus** จากไอเดียให้กลายเป็น **นวัตกรรมที่จับต้องได้จริง**
- มี **ฐานข้อมูลสำหรับวางแผนรับมือล่วงหน้า** — ข้อมูลย้อนหลังที่ต่อเนื่องคือสิ่งที่ทำให้การพยากรณ์เป็นไปได้

### ต่อสิ่งแวดล้อม

- ช่วยป้องกันการสูญเสียน้ำสะอาดที่ถูกใช้อย่างสิ้นเปลืองจากการรั่วซึมใต้ดิน
- เปลี่ยนจาก **"รอพังแล้วซ่อม"** เป็น **"แก้ก่อนที่มันจะสาย"**

### สรุปคุณค่าหลัก

> **เพิ่มความมั่นคง ลดการใช้งบประมาณ**
>
> ไม่ใช่แค่การประหยัด แต่เป็นการยกระดับสถานศึกษาให้มีความมั่นคง

---

## 14. ข้อจำกัดและแนวทางพัฒนาต่อ

### ข้อจำกัดปัจจุบัน

- **ไม่มีระบบผู้ใช้/การล็อกอิน** ทุก endpoint เปิดกว้าง (ยกเว้น `/update` ที่ตั้ง API key ได้) — เหมาะกับการใช้งานในวง LAN ที่ควบคุมได้เท่านั้น
- **สูตร NTU ยังต้องปรับเทียบซ้ำ** — สูตร `-1120.4·V² + 5742.3·V - 4353.8` ถูกจูนมาสำหรับเส้นทางอ่านค่าของ ESP8266 ไม่ใช่วงจรแบ่งแรงดันนี้ และ ADC ของ ESP32 มีความไม่เชิงเส้นที่ปลายช่วง
- **Google Sheets ยังไม่มีคอลัมน์ NTU** (มีคอลัมน์ Flow Rate แล้ว) การเพิ่มต้องแก้ schema ของ Apps Script และ deploy เวอร์ชันใหม่
- **ค่า TDS ในเส้นทางสำรอง** คำนวณบนบอร์ดด้วย k=1.0 จึงไม่สามารถย้อนปรับตามค่าคาลิเบรตส่วนตัวได้ภายหลัง (ต่างจากความขุ่นที่เก็บ ADC ดิบไว้จึงคำนวณใหม่ได้เสมอ)
- **ไม่มีชุดทดสอบอัตโนมัติ** ฝั่งแบ็กเอนด์
- เฟิร์มแวร์กับแบ็กเอนด์ **ไม่ซิงก์กันอัตโนมัติ** — เปลี่ยนสัญญา endpoint ที่ฝั่งหนึ่งต้องอัปเดตอีกฝั่งด้วยมือ

### แนวทางพัฒนาต่อ

- เพิ่มโมเดล AI สำหรับ **ตรวจจับความผิดปกติ (anomaly detection)** จากรูปแบบการใช้น้ำ เพื่อระบุจุดรั่วโดยอัตโนมัติ ซึ่งเป็นหัวใจของคำว่า "AI-Powered" ในชื่อโครงการ
- ขยายจุดติดตั้งเซนเซอร์หลายจุดในอาคาร เพื่อระบุตำแหน่งการรั่วให้แคบลงจากผลต่างของอัตราการไหลระหว่างจุด
- เพิ่มคอลัมน์ NTU และข้อมูลการไหลลงในสเปรดชีต
- เพิ่มระบบยืนยันตัวตนก่อนนำระบบออกสู่อินเทอร์เน็ตแบบเปิด

---

## ภาคผนวก: ตารางค่าที่ส่งใน `/update`

| ฟิลด์ | ความหมาย |
|---|---|
| `temperature` | อุณหภูมิน้ำ (°C) จาก DS18B20 |
| `turbidity` | ค่า NTU ที่ผ่านการปรับเทียบแล้ว หรือ ADC ดิบถ้ายังไม่ปรับเทียบ |
| `turbidityRaw` | ค่า ADC ดิบเสมอ (ใช้โดยหน้าคาลิเบรตและ Google Sheets) |
| `turbidityUnit` | `"NTU"` หรือ `"ADC"` — ฟรอนต์เอนด์ใช้ติดป้ายหน่วยให้ถูก |
| `turbidityNtu` | ค่า NTU ที่ปรับเทียบแล้ว หรือ `null` |
| `tdsVoltage` | แรงดันดิบจากเซนเซอร์ TDS |
| `tds` | ค่า ppm ที่ผ่านการปรับเทียบแล้ว |
| `ec` | ค่าการนำไฟฟ้า (µS/cm) คำนวณจาก `tds` — ไม่ใช่เซนเซอร์แยก แต่เป็นค่าเดียวกันก่อนคูณ 0.5 |
| `flowRate` | อัตราการไหลปัจจุบัน (ลิตร/นาที) |
| `waterUsageToday` | ปริมาณน้ำที่ใช้ไปวันนี้ (ลิตร) รีเซ็ตทุกวัน |
| `source` / `timestamp` | แหล่งที่มาและเวลา — `timestamp` ที่ส่งผ่าน WebSocket เป็น **epoch วินาที** (ต่างจากแถวใน `/history` และ SQLite ที่ใช้ **มิลลิวินาที**) |
